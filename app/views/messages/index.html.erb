<h1>メッセージ</h1>
<% @messages.each do |message| %>
    名前：<%= message.neme %>
    内容：<%= message.body %>
    <br>
<% end %>

</h1><%= form_for(Message.new) do |f| %>
    名前：
    <%= f.text_field :name %>
    
    内容：
    <%= f.text_field :body %>
    <%= f.submit %>
<% end %>